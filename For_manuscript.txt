The datatype must be set to DNA only (and not "standard" (i.e., micture of DNA and indels codes)).

My code does contain matrix checks (i.e., chek if charset range continuous; check if no range is overlapping, etc.)
